'use client';
import { motion } from 'framer-motion';

export const MidnightGlowProgress = ({ slides, currentSlide }: { slides?: any[], currentSlide: number }) => {
    // Hidden since the new Clock design takes up the bottom right corner and we paused rotation
    return null;
};
